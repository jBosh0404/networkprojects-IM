/**
 * 
 */
package jBosh0404.NetworkProjects.InstantMessenger;

/**
 * @author Joshua
 *
 */
public class IMClient {

	/* 
	 * TODO Will likely need an internal helper class to handle new user registration.
	 * Call this class RegForm
	 * 
	 * Will also likely need an internal helper class to handle an IM window. Each window should be handled inside of a separate
	 * thread, and each window should connect to one other user only. The constructor should accept, among other things, the recipient
	 * for all messages to be sent, which will be wrapped into a Message object at the time of sending. The thread should search a global
	 * incoming messages list for a message object from the outgoingRecipient/incomingSender associated with that thread, print the message
	 * to a text area, and then delete the message from the incomingMessages list.
	 */
	
	public IMClient() {
		
		
		
	}
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
